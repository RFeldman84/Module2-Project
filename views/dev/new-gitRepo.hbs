
<center><h3 class="head">NEW Github Repository Resource</h3></center>
<div class="form-page">
  <div class="form-icon-flex">  
    
    <img class="form-icon" src="/images/github-logo.png" alt="github icon">
    <div class="row form-row">
      <div class="col-md-12">
        <form class="add-form" action="/dev/newGitRepo/add" method="POST" enctype="multipart/form-data">
          <div class="form-group">
            <label> Repo Topic:
            {{#if errorMessage}}
            <i style="color: red">**required</i>
            {{else}} <i>*required</i>
            {{/if}}
              <input class="form-control" type="text" name="lang" value="{{lang}}">
            </label>
          </div>
          <div class="form-group">
            <label> Description/Languages/Keywords:
              <textarea class="form-control" name="description" cols="30" rows="4">{{description}}</textarea>
            </label>
          </div>
          <div class="form-group">
            <label> Repository:
            {{#if errorMessage}}
            <i style="color: red">**required</i>
            {{else}} {{#if errorInput}} <i style="color: red">*{{errorInput}}</i>
            {{else}} <i>*required</i>
            {{/if}}
            {{/if}}
              <input class="form-control" type="text" name="gitRepo" placeholder="<USERNAME>/<REPO>" value="{{gitRepo}}" >
            </label>
          </div>
          <input type="hidden" name="urlType" value="repo">
          <input type="hidden" name="resType" value="dev">
          <button type="submit" class="btn btn-primary">ADD RESOURCE</button>
          <br>
          {{#if errorMessage}} 
          <i style="color: red">*please fill in ALL required fields</i>
          {{/if}}
        </form>
      </div>
    </div>
  </div>
</div>