{{!-- <h2>ADD NEW FOLDER</h2>

<div class="row">
  <div class="col-md-6">
    <form action="/bookmarks/new-folder/create" method="POST">
      <div class="form-group">
        <label> Folder Name:
          <input class="form-control" type="text" name="name">
        </label>
        {{#if errorMessage}}
        <i style="color: red">*{{errorMessage}}</i>
        {{/if}}
      </div>
      <button type="submit" class="btn btn-primary">CREATE</button>
  </div>

  <div class="row">
    <div class="col-md-6">
      <form action="/bookmarks/new-folder/create" method="POST">
        <div class="form-group">
          <input class="form-control-sm" type="text" name="name" placeholder="enter new folder name">
          <button type="submit" class="btn-sm btn-primary">CREATE</button>
          {{#if errorMessage}}
          <i style="color: red">*{{errorMessage}}</i>
          {{/if}}
        </div>
      </form>
    </div> --}}
